# 实际测试输入；已移除服务端生成的 metadata，便于复现。
# CPU 为核；vgpu-memory 每单位代表 100 MiB；cores 为百分比。
apiVersion: v1
kind: ResourceQuota
metadata:
  name: cpu
  namespace: volcano-tests
spec:
  hard:
    requests.cpu: '1'
